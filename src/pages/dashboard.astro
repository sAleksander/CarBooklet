---
import AppLayout from "@/layouts/AppLayout.astro";
import DeadlineCard from "@/components/DeadlineCard.astro";
import LastEntryCard from "@/components/LastEntryCard.astro";
import { createClient } from "@/lib/supabase";
import { getCarById } from "@/lib/services/cars";
import { getCarDeadlines, getLastEntry } from "@/lib/services/entries";
import { getT } from "@/i18n/server";

const { user, selectedCarId, lang } = Astro.locals;
const t = getT(lang);

if (!selectedCarId) {
  return Astro.redirect("/cars");
}

const supabase = createClient(Astro.request.headers, Astro.cookies);

if (!supabase || !user) {
  return Astro.redirect("/cars");
}

let car;
let deadlines;
let lastEntry;
try {
  [car, deadlines, lastEntry] = await Promise.all([
    getCarById(supabase, selectedCarId, user.id),
    getCarDeadlines(supabase, selectedCarId, user.id),
    getLastEntry(supabase, selectedCarId, user.id),
  ]);
} catch {
  return Astro.redirect("/cars?error=load_failed");
}

if (car?.user_id !== user.id) {
  return Astro.redirect("/cars");
}
---

<AppLayout title={`${t("nav.dashboard")} — ${car.brand} ${car.model}`} selectedCar={car}>
  <div class="flex justify-center pt-4">
    <div class="w-full max-w-3xl">
      <div class="mb-8 rounded-2xl border border-white/10 bg-white/10 px-8 py-6 text-white backdrop-blur-xl">
        <h1 class="mb-1 bg-gradient-to-r from-blue-200 to-purple-200 bg-clip-text text-2xl font-bold text-transparent">
          {car.brand}
          {car.model}
        </h1>
        <p class="text-sm text-blue-100/60">{car.production_year}</p>
        <a
          href="/cars"
          class="mt-3 inline-block text-sm text-blue-200/70 underline-offset-2 hover:text-blue-200 hover:underline"
        >
          {t("dashboard.changeCar")}
        </a>
      </div>

      <div class="grid grid-cols-1 gap-4 md:grid-cols-3">
        <DeadlineCard title={t("dashboard.oilChange")} status={deadlines.oilChange.status}>
          {
            deadlines.oilChange.status === "no_data" ? (
              <p class="text-sm text-blue-100/50">{t("dashboard.noOilChange")}</p>
            ) : (
              <div class="space-y-1 text-sm">
                <p class="text-blue-100/70">
                  {t("dashboard.last")}{" "}
                  <span class="font-medium text-white">{deadlines.oilChange.lastConductedAt}</span>
                </p>
                <p class="text-blue-100/70">
                  {t("dashboard.nextDue")} <span class="font-medium text-white">{deadlines.oilChange.nextDueDate}</span>
                </p>
                {deadlines.oilChange.nextDueMileage !== null && (
                  <p class="text-blue-100/50">
                    {t("dashboard.orAtKm", { value: deadlines.oilChange.nextDueMileage.toLocaleString() })}
                  </p>
                )}
              </div>
            )
          }
        </DeadlineCard>

        <DeadlineCard title={t("dashboard.inspection")} status={deadlines.inspection.status}>
          {
            deadlines.inspection.status === "no_data" ? (
              <p class="text-sm text-blue-100/50">{t("dashboard.noInspection")}</p>
            ) : deadlines.inspection.status === "no_next_date" ? (
              <div class="space-y-1 text-sm">
                <p class="text-blue-100/70">
                  {t("dashboard.last")}{" "}
                  <span class="font-medium text-white">{deadlines.inspection.lastConductedAt}</span>
                </p>
                <p class="text-blue-100/50">{t("dashboard.nextDateNotSet")}</p>
              </div>
            ) : (
              <p class="text-sm text-blue-100/70">
                {t("dashboard.nextInspection")}{" "}
                <span class="font-medium text-white">{deadlines.inspection.nextInspectionDate}</span>
              </p>
            )
          }
        </DeadlineCard>

        <DeadlineCard title={t("dashboard.insurance")} status={deadlines.insurance.status}>
          {
            deadlines.insurance.status === "no_data" ? (
              <p class="text-sm text-blue-100/50">{t("dashboard.noInsurance")}</p>
            ) : (
              <div class="space-y-1 text-sm">
                <p class="text-blue-100/70">
                  {t("dashboard.renewal")} <span class="font-medium text-white">{deadlines.insurance.renewalDate}</span>
                </p>
                {deadlines.insurance.insurer && <p class="text-blue-100/50">{deadlines.insurance.insurer}</p>}
              </div>
            )
          }
        </DeadlineCard>
      </div>

      {
        lastEntry && (
          <div class="mt-4">
            <LastEntryCard entry={lastEntry} />
          </div>
        )
      }
    </div>
  </div>
</AppLayout>
